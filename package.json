{
    "scripts": {
        "clean": "node scripts/cleanCypressDirs.js",
        "test": "npm run clean && cypress open",
        "run": "npm run clean && cypress run",
        "cy:run": "cypress run",
        "cy:run:spec": "cypress run --spec $SPEC"
    },
    "devDependencies": {
        "cypress": "^15.7.1",
        "cypress-file-upload": "^5.0.8",
        "fs-extra": "^11.3.2",
        "rimraf": "^6.0.1"
    }
}
